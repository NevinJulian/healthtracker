name: Android Build Check

# Verifies the Android APK actually compiles on a real runner.
# The merge gate (typecheck + jest) is JS-only and cannot catch native/Gradle
# breakage (e.g. a reanimated <-> React Native C++ incompatibility), so this
# job builds the APK via EAS without publishing a release.
#
# Runs on PRs that touch dependency/native-affecting files, and on demand.

on:
  pull_request:
    branches:
      - main
    paths:
      - 'package.json'
      - 'package-lock.json'
      - 'app.json'
      - 'eas.json'
      - 'babel.config.js'
      - '.github/workflows/build-check.yml'
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # `tools` was removed from Google's SDK repository. Only platform-tools is
      # needed for `eas build --local`.
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: 'platform-tools'

      # Installed directly rather than via expo/expo-github-action — that action
      # installs through yarn, which fails on current runners (expo/eas-cli#2930).
      - name: Install EAS CLI
        run: npm install -g eas-cli@latest

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build APK locally via EAS (verification only)
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: eas build -p android --profile preview --local --non-interactive --output app-release.apk

      - name: Upload APK artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: app-release.apk
          if-no-files-found: error
