{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // TypeScript 6 no longer loads every @types package by default. The
    // tests need the jest and node globals.
    "types": ["jest", "node"]
  }
}
